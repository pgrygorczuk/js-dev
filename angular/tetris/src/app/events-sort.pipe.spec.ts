import { EventsSortPipe } from './events-sort.pipe';

describe('EventsSortPipe', () => {
  it('create an instance', () => {
    const pipe = new EventsSortPipe();
    expect(pipe).toBeTruthy();
  });
});
